#!/usr/bin/env node
/**
 * Temporary stub so `npm start`'s prestart hook succeeds even though the real
 * backfill worker hasn't shipped yet. When the actual implementation is ready,
 * replace this file with real logic (or point the package script elsewhere).
 */

console.log("[backfillDailySurprises] No-op stub: nothing to backfill yet.");
process.exit(0);
